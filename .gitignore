node_modules
desktop.ini
public/desktop.ini
main*
*.json
*-workspace
# Local Netlify folder
.netlify
.env
Archive
/netlify
.wrangler
.dev.vars
aira-forecaster-agents

# Markdown docs — only README tracked (CLAUDE.md is private, synced manually)
*.md
!README.md

# Local/integration tests — never commit
*.local.js
*.local.ts
TEST_REPORT.md
tests/local/

