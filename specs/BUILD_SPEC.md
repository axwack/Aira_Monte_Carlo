# Build Specification
Current feature: Corrected Roth Conversion Explorer with three-bucket withdrawal order.
Requirements: withdrawal order as above, only pre-tax draw as ordinary income, respect overrides, output Source column.
Acceptance: `npm test` passes + LOGIC_AUDIT.md confirms correctness.
