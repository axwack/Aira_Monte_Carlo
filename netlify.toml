[build]
  command   = "npm run build"
  publish   = "build"

[build.environment]
  NODE_VERSION        = "18"
  GENERATE_SOURCEMAP  = "false"

# Serve the SPA for all routes
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
